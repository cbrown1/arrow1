language: c++

sudo: false

before_install:
  - uname -a
  - free -m
  - df -h
  - ulimit -a

install:
  - sudo apt-get install libjack-dev libsndfile1-dev libboost-program-options-dev

script:
  - make

notifications:
  email: false
